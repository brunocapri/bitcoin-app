import 'package:flutter/material.dart';

class ProfitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Profit Page")),
    );
  }
}
